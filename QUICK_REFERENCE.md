# 快速参考卡片

## 📋 场景速查表

### 场景 1: 快速扫描 Class A 网段 (10.0.0.0/8)
```bash
python main.py -i 10.0.0.0/8 --mode network --threads 1000 --timeout 0.5
```
✅ **结果**：20 秒内发现所有活跃 /16 并细化到 /24  
💾 **数据包**：~2000 个数据包（节省 99.9%）

---

### 场景 2: 精确扫描单个 C 段 (192.168.1.0/24)
```bash
python main.py -i 192.168.1.0/24 --mode host --threads 2000 --timeout 0.3
```
✅ **结果**：逐 IP 发现所有活跃主机  
⏱ **耗时**：1-5 秒

---

### 场景 3: 扫描 B 段 (10.0.0.0/16)
```bash
python main.py -i 10.0.0.0/16 --mode network --threads 1000 --timeout 0.3
```
✅ **结果**：8 秒扫描 256 个 /24  
📦 **发包**：~2000 个

---

### 场景 4: 隐蔽探测（减少网络活跃度）
```bash
python main.py -i 10.0.0.0/12 --mode network --threads 100 --timeout 1.0
```
✅ **特点**：低并发，延长探测时间，降低被检测风险

---

## 🎯 核心优化原理

### 传统方案
- 10.0.0.0/8 = **16,777,216 个 IP** → **全部扫描** ❌
- 发包数：16M 个 → 网络风暴 → IDS 告警

### 我们的方案（二级抽样）
```
第一层：256 个 /16 × 4 样本 = 1,024 个包
    ↓
检测活跃 /16（假设 2 个）
    ↓
第二层：2 个 /16 × 256 个 /24 × 8 样本 = 4,096 个包
    ↓
总计：~5,120 个包 ✅
节省率：99.97%
```

---

## 📊 性能基准

| 网段 | 模式 | 耗时 | 发包数 | 发现数 |
|------|------|------|--------|---------|
| 10.0.0.0/8 | network | 20s | ~2000 | 2个/24 |
| 10.222.0.0/16 | network | 8.3s | ~2000 | 1个/24 |
| 192.168.1.0/24 | host | 0.5s | 254 | 3台主机 |

---

## 🚀 推荐工作流

```bash
# Step 1: 快速定位活跃 /16 (Class A)
python main.py -i 10.0.0.0/8 --mode network --threads 500 --timeout 0.5
# 输出：发现活跃网段 → 10.5.0.0/16, 10.221.0.0/16

# Step 2: 针对活跃的 /16 进一步细化到 /24
python main.py -i 10.5.0.0/16 --mode network --threads 500 --timeout 0.3
# 输出：发现活跃 /24 → 10.5.0.0/24, 10.5.1.0/24

# Step 3: 逐 IP 扫描确定所有活跃主机
python main.py -i 10.5.0.0/24 --mode host --threads 2000 --timeout 0.2
# 输出：所有活跃 IP → 10.5.0.1, 10.5.0.5, ...
```

---

## 🔧 高级配置

### 修改探测端口
编辑 `goscan/core/discovery.py` 第 12 行：
```python
self.check_ports = [445, 80, 22, 3389]  # 添加 SSH, RDP
```

### 修改采样 IP
编辑 `_detect_networks_direct()` 方法中的 `samples` 列表：
```python
samples = [1, 2, 254, 7, 8, 9, 100, 200, 50]  # 增加采样点
```

### 自定义 /16 抽样
编辑 `_detect_large_networks()` 方法中的 `samples`：
```python
samples = [1, 2, 254, 255]  # /16 级别采样
```

---

## ⚠️ 注意事项

1. **网络权限**：需要对目标网段有通信权限（内网使用）
2. **IDS 检测**：虽然发包少，但 TCP 连接日志仍会被记录
3. **超时调整**：网络延迟高时增加 `--timeout`（建议 0.5-1.0）
4. **线程数**：并发太高可能导致本地网卡饱和，建议 500-1000
5. **可靠性**：单个 IP 探测失败可能被漏掉，建议重复探测或增加采样点

---

## 🎓 实现原理

### 存活检测
- 使用 TCP SYN 连接探测（轻量级，不完全连接）
- 默认端口：445（SMB）和 80（HTTP）
- 任意端口通过即视为主机存活

### 网段检测
- 自动判断网段大小
- 小于 /24：直接全扫
- /24-/16：中等粒度扫描
- /16 以上：两级抽样法

### 并发优化
- 1000+ 线程异步探测
- 尽快返回（找到活跃即停止尝试其他端口）
- 随机打乱扫描顺序（防止网络突发）

---

## 📝 日志导出

保存扫描结果到文件：
```bash
python main.py -i 10.0.0.0/12 --mode network 2>&1 | tee results.txt
```

解析输出：
```bash
# 仅提取 /24 网段
grep "Active Subnet" results.txt | awk '{print $3}'

# 仅提取最终结果
grep "/24" results.txt | tail -20
```

---

## 📌 版本信息

- **工具名**：Py-Internal-Scan（内网大网段优化扫描）
- **版本**：1.1（支持二级抽样）
- **Python**：3.7+
- **依赖**：内置库（无外部依赖）
- **许可**：MIT

---

**欢迎在实际项目中使用！** 🚀
